import { LoopRangePipe } from './loop-range.pipe';

describe('LoopRangePipe', () => {
  it('create an instance', () => {
    const pipe = new LoopRangePipe();
    expect(pipe).toBeTruthy();
  });
});
