import { ImageThumbnailDirective } from './image-thumbnail.directive';

describe('ImageThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageThumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
